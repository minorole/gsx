name: Update Homebrew Formula

on:
  release:
    types: [published]

jobs:
  update-formula:
    runs-on: ubuntu-latest
    steps:
      - name: Get release info
        id: release
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"  # Strip leading 'v'
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

          TARBALL_URL="https://github.com/${{ github.repository }}/archive/refs/tags/${{ github.event.release.tag_name }}.tar.gz"

          # Download and calculate SHA256
          SHA256=$(curl -sL "${TARBALL_URL}" | sha256sum | cut -d' ' -f1)
          echo "sha256=${SHA256}" >> $GITHUB_OUTPUT

      - name: Checkout homebrew-tap
        uses: actions/checkout@v4
        with:
          repository: minorole/homebrew-tap
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}

      - name: Update formulas
        run: |
          VERSION="${{ steps.release.outputs.version }}"
          SHA256="${{ steps.release.outputs.sha256 }}"

          for formula in Formula/gpane.rb Formula/gsx.rb; do
            sed -i "s/sha256 \".*\"/sha256 \"${SHA256}\"/" "$formula"
            sed -i "s|/v[0-9]*\.[0-9]*\.[0-9]*\.tar\.gz|/v${VERSION}.tar.gz|" "$formula"
          done

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/gpane.rb Formula/gsx.rb
          git diff --cached --quiet || git commit -m "gpane ${VERSION}"
          git push
        env:
          VERSION: ${{ steps.release.outputs.version }}
